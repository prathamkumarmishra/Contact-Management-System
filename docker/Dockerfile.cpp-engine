# C++ Engine Dockerfile
FROM gcc:13 AS build

WORKDIR /app

RUN apt-get update && apt-get install -y cmake

COPY . .

RUN mkdir build && cd build && cmake .. && cmake --build .

FROM debian:bookworm-slim

WORKDIR /app

COPY --from=build /app/build/contact-engine .

CMD ["./contact-engine"]
